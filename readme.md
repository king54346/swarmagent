分解任务的关键原则：以 context 为边界，而非以职责类型为边界，当我看见这句话的时候虎躯一震，我们不应以人类的职责分工去让agent也已按照同样思想理念去照搬工作模式，我们应该深入去理解agent或者是说大模型底层的工作原理去合理去任务分配 所以这个项目突然就变得索然无味，或者说我还没找到一种合适的方法和理念去按照适合agent的方式去组织工作

todolist：
1. 使用agentscope重构
2. Verification Subagent 模式 专门设计一个只负责"验证"的 subagent（跑测试、检查合规、校验输出），因为验证天然不需要完整的实现上下文，是少数几个分工清晰、handoff 成本低的场景
3.   虚拟文件空间 (需要沙箱后端Daytona)
2.   界面bug 
3.   删除agent
4，  删除聊天组
5.   组内停止聊天
6.   错误消除
7.   指令
8.   可以预设agent并使用
9.   每个agent应该具备todolist任务，每个list都需要有status(pending or complete)
10.   实现memeory系统（ 记忆检索排序算法的评分公式，交叉编码器重排序，memoryscope隔离，近期经验加成，重要性权重，长度归一化，时间衰减，最低分过滤，MMR 多样性去重，自适应检索）
